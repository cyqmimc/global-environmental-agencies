# 数据更新检查记录（2026-09-24）

## 执行结果

完整执行修复后的 `npm run update-all`：源数据合并 → 世界银行拉取 → UN SDG 拉取 → 拆分与 OG 数据 → schema 校验 → 源文件一致性校验 → 时效检查，退出码为 0。

- WB：80/80 国；67 国 IQAir 2024 PM2.5 覆盖值保留。
- SDG：水压力 80/80（2023）；单位 GDP 物质消费 80/80（2022）；海洋 KBA 69/80（2025）；土地退化 49/80（2019）。
- 与运行前版本比较：上述官方观测值和历史序列未变化；刷新获取时间，新增 WB 人均 CO₂ 数据年份，查询上限从固定 2025 改为当年。
- 71 项测试通过，ESLint、生成文件一致性和生产构建通过；产出 160 个国家语言页面及公共 API。
- 浏览器首页可加载 80 国，页脚显示获取日期 2026-09-24。未做全站交互回归；未部署到线上。

## 已修复

1. SDG 将 null、空字符串、空白字符串转换为 0：现在排除无效观测，同时保留真实零值并检查年份。
2. API 异常分页静默结束，可能写入不完整数据：WB / SDG 现在要求有效页数及非空数据页；SDG 任一指标零覆盖时拒绝写入。
3. WB 网络请求缺少超时和重试：请求及响应体解析纳入 30 秒超时，最多重试两次。
4. 人均排放回退计算混用最新人口与不同年份排放：现在仅用排放年份的人口，并保存对应年份；公共 API 增加 co2PerCapitaYear。
5. 一键更新中途失败留下部分新文件：运行前保存 7 份输出快照，任一步骤报错时恢复；新增源文件一致性校验。
6. schema 校验遇到缺失或损坏的 WB/SDG JSON 返回成功：现在以非零退出码阻止通过。
7. WB 查询截止年固定为 2025：现在随 UTC 年份推进。
8. 维护文档将碳定价维护入口指向生成文件：改为源文件目录。

新增回归测试覆盖无效值、分页、人均排放年份、失败回滚、响应解析重试及三类快照缺失/损坏。

## 优化建议（按优先级）

| 优先级 | 建议 | 本次依据与验收方向 |
|---|---|---|
| P1 | 将数据获取时间、观测年份、来源和统计口径分别保存到每项观测 | 最新抓取不代表最新观测；PM2.5 为 67 国 IQAir 2024 与 13 国 WB 2023 混合。支持按相同年份/来源筛选，明确混合比较提示。 |
| P1 | 清理 legacyData 告警，将旧手工值归档并记录原口径 | audit-drift 在 72/80 国超阈值（17 国森林、71 国碳排）；schema 另有 78 条差异警告。前端主要已使用 WB；先核对口径，不自动用新数据改写历史记录。 |
| P1 | 人工核验公约状态并记录核验日期 | 当前项目记录有 17 国 NDC 3.0 待提交、9 国 BTR 待提交；这不是本次官方注册表的新核验结果。自动 WB/SDG 更新不会刷新这些策展字段。 |
| P2 | 增加覆盖率下降、年份倒退、异常跃变门禁 | 目前能拦截空数据和异常分页，但有效响应仍可能出现部分国家丢失。比较前后各指标覆盖率、年份和变化幅度，生成可审阅差异摘要。 |
| P2 | 使用暂存目录、版本快照和互斥锁 | 当前回滚覆盖普通步骤失败；强制终止及并行更新仍有风险。所有文件校验后统一发布，禁止并行写入。 |
| P2 | 固化数据顺序、减少无变化更新噪声 | 本轮观测值未变，但获取时间及对象顺序仍导致大量文本 diff。排序输出，区分检查时间与内容变化时间，PR 聚焦实际变化。 |
| P2 | 浏览器数据缓存按快照版本协调 | Service Worker 分别刷新多个 JSON，已有页面可能短暂混用版本；SDG 未列入数据缓存名单。用统一版本清单组织加载，并验证离线场景。 |

时效检查仍有 9 条需关注项，包括可再生能源主要为 2021 年、土地退化为 2019 年，以及 GDP/PM2.5 年份不一致。这些问题不能仅靠重新抓取解决。

## 使用

```bash
npm run update-all
npm test
npm run lint
npm run build
```

运行更新期间不要并行执行另一轮更新或构建。单独的 fetch-data / fetch-sdg-data 命令不提供整条流水线回滚。
